{
  "name": "durable-agent-substrate",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "pnpm run check:specs && pnpm run check:docs && pnpm -r run check",
    "check:specs": "ruby -e 'require \"yaml\"; ARGV.each { |f| YAML.load_file(f); puts \"ok #{File.basename(f)}\" }' features/durable-agent-substrate/*.feature.yaml",
    "check:docs": "find README.md docs features -type f \\( -name '*.md' -o -name '*.yaml' \\) -print0 | xargs -0 perl -ne 'print \"$ARGV:$.: trailing whitespace\\n\" if /[ \\t]$/; close ARGV if eof' && ! grep -R -n -E '<<<<<<<|>>>>>>>|=======' README.md docs features",
    "test": "pnpm -r run test"
  }
}

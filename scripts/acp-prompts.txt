# Firegrid ACP tool-elicitation matrix.
# One prompt per line. Blank lines and lines starting with # are ignored.
# Each line is a separate agent turn, driven headlessly via scripts/acp-drive.mjs.
# Edit freely. Keep prompts explicit about which tool to use so coverage is deliberate.

# --- introspection (no tool call) ---
list every tool you have available, with a one-line purpose each

# --- sleep: DurableClock suspend/resume ---
use your sleep tool to durably suspend for 2 seconds, then tell me you woke up

# --- child session: spawn + prompt (the per-context observer path) ---
use session_new to spawn a child session, then use session_prompt to ask that child to reply with the single word "pong", then report exactly what happened

# --- wait_for: durable ingress block + wake ---
use wait_for to block on a session.self.checkpoint event with a 5 second timeout, and report the row you receive or the timeout

# --- NOTE: schedule_me is intentionally omitted ---
# It blocks the calling turn until the scheduled time and times out the edge
# (tf-uoga / #632; fix in PR #637). Re-add a SHORT one to verify once #637 lands:
# use schedule_me to schedule a prompt to yourself ~15 seconds from now that says "scheduled fired"

# Alignment Audit A: Core / Composition

Date: 2026-06-02
Main audited: `da2d4104a0cefafc560600eeedd2b5cd5631d569`

Scope: align load-bearing composition SDDs against the post-section-12 runtime
factory and the factory vision. Target architecture for this audit:
`FiregridRuntime(spec, adapter)`, Durable Streams floor, protocol read views,
authority collapse, and a generic substrate that lets consumers compose
factory choreography without Firegrid shipping a factory workflow.

## Per-SDD Verdicts

| SDD | Verdict |
| --- | --- |
| `docs/sdds/Firegrid Composition-Type-Driven-Greenfield-SDD.md` | The named file is absent on main. The implemented section-12 target exists in code and is tracked by `tf-0awo.23` / `tf-0awo.27`, but the SDD pointer is stale. |
| `docs/cannon/sdds/SDD_FIREGRID_SCHEMA_PROJECTION_CONTRACT.md` | Mostly aligned post-#765: protocol schemas are the source, toolkit projection is real, input-intent bridge text is refreshed. Two material residuals remain: client read-path durable table facades are still live and tracked by `tf-0awo.6` / `tf-ll90.8.3`; `execute` / `send` / `call` are advertised but not lowered by the unified MCP executor. |
| `docs/cannon/sdds/SDD_FIREGRID_SCHEMA_PROJECTION_IMPLEMENTATION.md` | Partially implemented and partially stale: `projectTool` and the operation-id uniqueness gate landed, but the doc still cites the old uniqueness-test path and still lists client read-path cleanup as open. |
| `docs/cannon/sdds/SDD_FIREGRID_ONE_SUBSTRATE_WORKFLOW_ENGINE.md` | Superseded by the unified signal/runtime-context architecture. The high-level "one substrate" direction holds, and `durable-tools/` is gone, but the exact `WaitForWorkflow` / `host-sdk/runtime-context-workflow-core.ts` implementation plan is not the current code path. |
| `docs/cannon/sdds/SDD_FIREGRID_RUNTIME_CONTEXT_INPUT_WRITE_ARM_MIGRATION.md` | Superseded in current production input delivery by `SignalTable` + `sendSignal` / `armSession`; its production table-input follow-on is still tracked by blocked `tf-vrz6`. |
| `docs/cannon/sdds/SDD_FIREGRID_ENGINE_NATIVE_PRIMITIVES_ESCAPE_HATCH.md` | Aligned as a contingency SDD, not active implementation. It is tracked by deferred `tf-0mt5`, with closure-path beads such as `tf-wunq`; two named structural collapse targets (`tf-ho99`, `tf-ws2x`) are closed/superseded. |

## Findings

SECTION-12 RUNTIME FACTORY IS IMPLEMENTED - EVIDENCE(`packages/runtime/src/unified/host.ts:219` table floor; `packages/runtime/src/unified/host.ts:261` engine floor; `packages/runtime/src/unified/host.ts:328` `FiregridRuntime(spec, adapter)`; `packages/runtime/src/unified/host.ts:336` adapter provided by merged floor with `Layer.orDie`; `packages/runtime/src/unified/host.ts:379` old `FiregridHost` wrapper delegates) - CLASSIFICATION(aligned) - DISPOSITION(supports the target) - tracked-by(`tf-0awo.23`, `tf-0awo.27`)

SECTION-12 SDD POINTER IS STALE / MISSING - EVIDENCE(`docs/findings/tf-0awo-18-modularity-compile-spike.md:5` already records that `docs/sdds/Firegrid Composition-Type-Driven-Greenfield-SDD.md` was not present; `docs/sdds/SDD_FIREGRID_RUNTIME_COMPOSITION_ERGONOMICS.md:65` is a different `Firegrid.composeRuntime` helper contract, not the section-12 greenfield cutover) - CLASSIFICATION(stale) - DISPOSITION(supports the target but weakens auditability) - SUGGESTED BEAD(`Restore or retire the missing section-12 composition SDD pointer`, P2)

DURABLE STREAM URL / NAME AUTHORITY IS FORWARD-ENCODED THROUGH THE CANONICAL ENCODER - EVIDENCE(`packages/protocol/src/launch/authority.ts:225` `durableStreamUrl`; `packages/protocol/src/launch/authority.ts:237` control-plane stream URL delegates to it; `packages/protocol/src/launch/authority.ts:244` output stream URL delegates to it; `packages/runtime/src/unified/host.ts:241` signal table uses `durableStreamUrl`; `packages/runtime/src/unified/host.ts:263` engine stream uses `durableStreamUrl`) - CLASSIFICATION(aligned) - DISPOSITION(supports the target) - tracked-by(`tf-0awo.27`)

PROTOCOL READ VIEWS EXIST, INCLUDING LOGS AS AN OUTPUT-TABLE FAMILY - EVIDENCE(`packages/protocol/src/launch/views.ts:14` contexts view; `packages/protocol/src/launch/views.ts:25` events view; `packages/protocol/src/launch/views.ts:31` logs view; `packages/protocol/src/launch/table.ts:193` log row family; `packages/protocol/src/launch/table.ts:201` `logs` collection; `packages/protocol/src/launch/schema.ts:317` stderr journal rule targets `logs`) - CLASSIFICATION(aligned) - DISPOSITION(supports the target) - tracked-by(`tf-0awo.23`)

CLIENT READ PATH STILL RESOLVES DURABLE TABLE FACADES - EVIDENCE(SDD `docs/cannon/sdds/SDD_FIREGRID_SCHEMA_PROJECTION_CONTRACT.md:149` names `tf-ll90.8.3` as the open boundary gap; `packages/client-sdk/src/firegrid.ts:17` imports `RuntimeControlPlaneTable`; `packages/client-sdk/src/firegrid.ts:18` imports `RuntimeOutputTable`; `packages/client-sdk/src/firegrid.ts:377` exports `FiregridRuntimeTables`; `packages/client-sdk/src/firegrid.ts:932` resolves the control table; `packages/client-sdk/src/firegrid.ts:933` resolves the output table; `packages/client-sdk/src/firegrid.ts:1531` builds the control table layer; `packages/client-sdk/src/firegrid.ts:1548` builds the output table layer; bead `tf-0awo.6` is open and `tf-ll90.8.3` is deferred) - CLASSIFICATION(boundary-violation) - DISPOSITION(gates the projection-boundary target) - tracked-by(`tf-0awo.6`, `tf-ll90.8.3`)

TOOLKIT PROJECTION HELPER AND OPERATION-ID GATE ARE IMPLEMENTED - EVIDENCE(`packages/runtime/src/unified/mcp-host/toolkit.ts:119` single `AGENT_TOOL_GROUPS` list; `packages/runtime/src/unified/mcp-host/toolkit.ts:168` `projectTool` reads projection metadata; `packages/runtime/src/unified/mcp-host/toolkit.ts:227` maps groups through `projectTool`; `packages/protocol/test/projection/operation-id-uniqueness.test.ts:102` projection contract test; `packages/protocol/test/projection/operation-id-uniqueness.test.ts:125` duplicate operation-id assertion) - CLASSIFICATION(aligned) - DISPOSITION(supports the target) - tracked-by(`tf-0awo.3`)

SCHEMA PROJECTION IMPLEMENTATION SDD HAS STALE FILE-LEVEL ACCEPTANCE - EVIDENCE(SDD `docs/cannon/sdds/SDD_FIREGRID_SCHEMA_PROJECTION_IMPLEMENTATION.md:286` expects `protocol/test/catalog-uniqueness.test.ts`; current gate is `packages/protocol/test/projection/operation-id-uniqueness.test.ts:1`; SDD `docs/cannon/sdds/SDD_FIREGRID_SCHEMA_PROJECTION_IMPLEMENTATION.md:289` still lists client read-path cleanup, matching the live leak above) - CLASSIFICATION(stale) - DISPOSITION(supports the target) - SUGGESTED BEAD(`Refresh schema projection implementation SDD paths and completed slices`, P3)

AGENT TOOLKIT ADVERTISES `send` / `execute` / `call` BUT THE UNIFIED EXECUTOR DOES NOT LOWER THEM - EVIDENCE(factory vision requires `execute` at `docs/vision/factory-vision.md:127`; schema advertises `execute` at `packages/protocol/src/agent-tools/schema.ts:700`; toolkit includes `send`, `execute`, and `call` in `packages/runtime/src/unified/mcp-host/toolkit.ts:136`, `packages/runtime/src/unified/mcp-host/toolkit.ts:157`, and `packages/runtime/src/unified/mcp-host/toolkit.ts:160`; the dispatcher switch handles only sleep/wait/session cases through `packages/runtime/src/unified/mcp-host/tool-dispatch.ts:526`-`597`; default unported-tool failure is `packages/runtime/src/unified/mcp-host/tool-dispatch.ts:598`; `tf-0awo.32` is closed after session tool lowering, but source still lacks these three arms) - CLASSIFICATION(gap) - DISPOSITION(gates factory capability 6 and any channel action tool using the public MCP surface) - SUGGESTED BEAD(`Lower send / execute / call in the unified MCP executor or remove them from the advertised toolkit profile`, P0)

ONE-SUBSTRATE SDD EXACT `WaitForWorkflow` PLAN IS SUPERSEDED - EVIDENCE(SDD `docs/cannon/sdds/SDD_FIREGRID_ONE_SUBSTRATE_WORKFLOW_ENGINE.md:87` names `packages/runtime/src/agent-tools/WaitForWorkflow.ts`; that path is absent from current source; SDD `docs/cannon/sdds/SDD_FIREGRID_ONE_SUBSTRATE_WORKFLOW_ENGINE.md:124` cites `tool-use-to-effect.ts`; that path is absent from current source; current MCP-entry wait lowers inside `packages/runtime/src/unified/mcp-host/tool-dispatch.ts:229` through channel streams and `Clock.sleep`; current tool dispatch is wrapped by `McpToolDispatchWorkflow` at `packages/runtime/src/unified/mcp-host/tool-dispatch.ts:655` and an `Activity.make` at `packages/runtime/src/unified/mcp-host/tool-dispatch.ts:666`; bead `tf-auuv` is closed as completed/superseded) - CLASSIFICATION(superseded) - DISPOSITION(supports the one-substrate direction but off-path for exact implementation) - tracked-by(`tf-auuv`, `tf-xw0w`, `tf-0mt5`)

RUNTIME INPUT MAILBOX IS DELETED FROM THE LIVE CONTROL TABLE, BUT THE PRODUCTION TABLE-INPUT FOLLOW-ON REMAINS TRACKED - EVIDENCE(SDD legacy path at `docs/cannon/sdds/SDD_FIREGRID_RUNTIME_CONTEXT_INPUT_WRITE_ARM_MIGRATION.md:17`-`25`; current unified index says no `inputIntents` at `packages/runtime/src/unified/index.ts:11`-`15`; current control plane collapsed `inputIntents` at `packages/protocol/src/launch/table.ts:168`-`176`; current signal sender records and arms at `packages/runtime/src/unified/signal.ts:193`; current session input signal uses `sendSignal` plus `armSession` at `packages/runtime/src/unified/channel-bindings.ts:126`-`152`; runtime body reads signals and sends adapter input at `packages/runtime/src/unified/subscribers/runtime-context.ts:114` and `packages/runtime/src/unified/subscribers/runtime-context.ts:139`; `tf-c9r9` is closed, `tf-vrz6` is blocked) - CLASSIFICATION(superseded) - DISPOSITION(supports the target; remaining production table-input work is tracked) - tracked-by(`tf-c9r9`, `tf-vrz6`)

ENGINE-NATIVE PRIMITIVES ARE A TRACKED CONTINGENCY, NOT A CURRENT UNTRACKED GAP - EVIDENCE(SDD trigger list at `docs/cannon/sdds/SDD_FIREGRID_ENGINE_NATIVE_PRIMITIVES_ESCAPE_HATCH.md:487`-`514`; first work package at `docs/cannon/sdds/SDD_FIREGRID_ENGINE_NATIVE_PRIMITIVES_ESCAPE_HATCH.md:542`-`565`; bead `tf-0mt5` is deferred; bead `tf-wunq` tracks the wait timeout/recycle closure path; beads `tf-ho99` and `tf-ws2x` are closed for the named structural targets) - CLASSIFICATION(aligned) - DISPOSITION(supports the target as a contingency anchor) - tracked-by(`tf-0mt5`, `tf-wunq`, `tf-ho99`, `tf-ws2x`)

FACTORY VISION IS STILL GENERIC-SUBSTRATE ALIGNED, BUT ITS CURRENT-STATE SUMMARY IS STALE - EVIDENCE(vision requires choreography and primitives at `docs/vision/factory-vision.md:52`-`83`; generic substrate/no factory workflow at `docs/vision/factory-vision.md:142`-`146`; old current-state summary at `docs/vision/factory-vision.md:466`-`489` still cites older capability-map beads; current capstone finding shows `session_new` now delegates at `docs/findings/tf-0awo.30-factory-capstone-sim.md:35`, while the executor gap above shows `execute` still does not dispatch) - CLASSIFICATION(stale) - DISPOSITION(supports the target but gates factory-proof interpretation) - SUGGESTED BEAD(`Refresh factory vision capability-state section after post-831 capstone rerun and execute-gap audit`, P2)

## Untracked Work

1. `send` / `execute` / `call` lowering is the only verified untracked blocker found in this audit. It is not sufficiently covered by closed `tf-0awo.32` because source still falls through to the unported-tool default for those names.
2. The missing section-12 composition SDD pointer is documentation/auditability work, not product behavior work.
3. The schema projection implementation SDD path refresh is documentation/auditability work.
4. The factory vision current-state refresh is documentation/auditability work.

## Stale / Superseded Reference List

- `docs/sdds/Firegrid Composition-Type-Driven-Greenfield-SDD.md` is not present on main; current implemented composition evidence lives in `packages/runtime/src/unified/host.ts`.
- `docs/cannon/sdds/SDD_FIREGRID_SCHEMA_PROJECTION_IMPLEMENTATION.md:286` names `protocol/test/catalog-uniqueness.test.ts`; current test is `packages/protocol/test/projection/operation-id-uniqueness.test.ts`.
- `docs/cannon/sdds/SDD_FIREGRID_ONE_SUBSTRATE_WORKFLOW_ENGINE.md` references deleted / superseded source paths: `packages/runtime/src/agent-tools/WaitForWorkflow.ts`, `tool-use-to-effect.ts`, and `packages/host-sdk/src/host/runtime-context-workflow-core.ts`.
- `docs/cannon/sdds/SDD_FIREGRID_RUNTIME_CONTEXT_INPUT_WRITE_ARM_MIGRATION.md` describes the legacy `RuntimeControlPlaneTable.inputIntents` / `DurableDeferred` mailbox as current production state; current production input delivery is unified signals.
- Runtime docs outside this audit still contain stale references, e.g. `packages/runtime/src/tables/README.md:16`-`18` describing `RuntimeControlPlaneTable.inputIntents`; this is not in the requested SDD set but should be cleaned during the next runtime-doc refresh.

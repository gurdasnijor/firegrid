# tf-sunc in_review bead reconciliation

Investigation date: 2026-06-03.

Scope: read-only reconciliation of four beads left `IN_REVIEW` since 2026-06-01. I did not mutate Beads state.

Method:

- `BEADS_DIR=$HOME/gurdasnijor/.beads br show <id>` for each bead.
- `gh pr list --state all --search "<id>"` plus `gh pr list --state all --limit 300 | grep -i "<id>"`.
- Source verification on current `main` / `origin/main` by grepping the protocol/runtime/source tree and checking PR commit ancestry.

Epistemic tiers:

- **Source-verified**: current checkout/source or git ancestry proves the fact.
- **PR/bead-inferred**: GitHub PR metadata or bead comments provide context, but the behavior is not present on `main`.

| Bead | Bead status / claim | Source-verified reality | PR / branch evidence | Classification | Recommendation |
|---|---|---|---|---|---|
| `tf-r06u.41` | `IN_REVIEW`, assignee `Agent3`, label `pr-779`; claim: add `ToolResult` arm to `RuntimeAgentOutputObservationSchema` carrying Effect AI `Prompt.ToolResultPart` and supplemental `toolUseId` / `toolName`. | **Source-verified: not landed.** `RuntimeAgentOutputObservationSchema` still enumerates only `Ready`, `TextChunk`, `ToolUse`, `PermissionRequest`, `TurnComplete`, `Status`, `Error`, and `Terminated`; no `ToolResult` arm appears in the runtime output observation union (`packages/protocol/src/session-facade/schema.ts:276`). `AgentOutputEventSchema` likewise omits `ToolResult` from the output event union (`packages/protocol/src/agent-output/schema.ts:92`). `ToolResult` exists only as an input event in runtime contract code, which does not satisfy this bead (`packages/runtime/src/events/contract.ts:47`). | **PR/bead-inferred:** PR #779 is a closed draft, `mergedAt=null`, base `sim/unified-kernel-validation`, head `codex/tf-r06u.41-toolresult-arm`. Its implementation commit `4601953774c5afe470db332dd57af16d76c8d65f` is not an ancestor of `origin/main`; the remote branch still exists. | `genuinely-OPEN-with-stale-PR` | Move the bead out of `in_review` back to `ready` (or equivalent open state). Reopen/retarget PR #779 or reimplement against current `main`; do not close as merged. |
| `tf-r06u.42` | `IN_REVIEW`, assignee `Agent3`, label `pr-780`; claim: host intent observer bridges append-intent stream records to `promptScoped` / `respondScoped`. | **Source-verified: not landed.** There is no `packages/runtime/src/unified/intent-observer.ts` and no `IntentRecordSchema` / `dispatchIntent` / `runIntentObserver` symbols in `packages/`. Current host composition wires `JournalObserverLive` only, not an intent observer (`packages/runtime/src/unified/host.ts:305`). The launch authority segment set has no intent stream segment (`packages/protocol/src/launch/authority.ts:42`). | **PR/bead-inferred:** PR #780 is a closed draft, `mergedAt=null`, base `codex/tf-r06u.33-edge-auth-scoping`, head `codex/tf-r06u.42-intent-observer`. Its commit `c248e74faed54ef58d80289043deac3e604cc923` is not an ancestor of `origin/main`; the remote branch still exists. The bead comment also says the PR deferred wiring/topology decisions, so even the closed draft was not full end-to-end completion. | `genuinely-OPEN-with-stale-PR` | Move back to `ready`, but keep the deferred wiring/topology caveat visible. Best next action is likely a reframe/split: standalone observer core, host-wide vs per-context intent stream topology, and host composition/read-cursor wiring. |
| `tf-r06u.43` | `IN_REVIEW`, assignee `Agent3`, label `pr-781`; claim: output retention floor plus 410-resync snapshot-offset entry point. | **Source-verified: not landed.** No `EdgeAuthConfig`, `outputRetentionFloorSeconds`, `snapshotOffset`, `/resync`, or resolver `resync` symbols are present in `packages/`. Current durable-stream HTTP handling maps status `410` to a bare `Gone({ url })`; no snapshot offset is parsed or returned there (`packages/effect-durable-streams/src/protocol/Http.ts:92`). | **PR/bead-inferred:** PR #781 is a closed draft, `mergedAt=null`, base `codex/tf-r06u.33-edge-auth-scoping`, head `codex/tf-r06u.43-retention-resync`. Its commit `a894b83b549c594a4776e27cb0bbb51210d0fcbd` is not an ancestor of `origin/main`; the remote branch still exists. | `genuinely-OPEN-with-stale-PR` | Move back to `ready`. Reopen/retarget PR #781 only after resolving whether the missing edge-auth package from `tf-r06u.33` should still be the target surface. |
| `tf-r06u.33` | `IN_REVIEW`, assignee `Agent3`, label `pr-777`; claim: scoped per-stream capability-token / edge auth-scoping package with token to opaque-handle grants (`open`, `append:intent`, `read:output`). | **Source-verified: not landed.** No `@firegrid/edge-auth` package exists under `packages/`, and no `edge-auth`, `EdgeAuth`, grant, denylist, `append:intent`, or `read:output` implementation symbols are present in `packages/`. The workspace includes `packages/*` (`pnpm-workspace.yaml:1`), and the current package set has no edge-auth package. The protocol host stream segment set also lacks the claimed intent stream authority helper that PR #777 said it added (`packages/protocol/src/launch/authority.ts:42`). | **PR/bead-inferred:** PR #777 is a closed draft, `mergedAt=null`, base `sim/unified-kernel-validation`, head `codex/tf-r06u.33-edge-auth-scoping`. Its commit `e98bb3370cf03766954b88ba507b6c42d52b272b` is not an ancestor of `origin/main`; the remote branch still exists. | `genuinely-OPEN-with-stale-PR` | Move back to `ready`. Because `.42` and `.43` were stacked on this branch, treat `.33` as the prerequisite stale PR to resolve first. Do not close as merged. |

## Summary

All four beads are `IN_REVIEW`, but none of their referenced implementation commits is in `origin/main`, all four referenced PRs are closed drafts with `mergedAt=null`, and current source lacks the claimed behavior. The safest reconciliation is to reopen these beads to `ready` (or an equivalent open state) rather than closing them.

The dependency order still matters: reconcile `tf-r06u.33` first, then reassess `tf-r06u.42` and `tf-r06u.43`, because both were stacked on the edge-auth branch. `tf-r06u.41` is independent of that stack but still not landed.

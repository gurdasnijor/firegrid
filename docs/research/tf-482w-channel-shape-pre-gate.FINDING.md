# Verdict: B - tf-kddg partial

Source read was performed on `origin/main` at `b27bce431f3bfac7c438931d38a1fbd88f04b54f`.

`channel.ts` has not reached the full tf-kddg target shape. It still defines and exports a generic `ChannelInventory` service, `makeChannelInventory`, `ChannelInventoryLive`, and string-target `findChannel` lookup (`packages/host-sdk/src/host/channel.ts:115`, `packages/host-sdk/src/host/channel.ts:119`, `packages/host-sdk/src/host/channel.ts:123`, `packages/host-sdk/src/host/channel.ts:129`, `packages/host-sdk/src/host/channel.ts:134`). The host export surface also still exports those inventory primitives (`packages/host-sdk/src/host/index.ts:8`). Runtime tool dispatch resolves channel strings through `ChannelInventory` and `findChannel` (`packages/host-sdk/src/agent-tools/execution/tool-use-to-effect.ts:235`), and MCP metadata enrichment reads `ChannelInventory.channels` (`packages/host-sdk/src/host/mcp-channel-metadata.ts:129`).

## Per-channel status

| Channel / family | Direction | Status | Evidence |
| --- | --- | --- | --- |
| `session.self.lifecycle` | ingress | Tag + Layer exists, but mirrored into `ChannelInventory` | Target + tag are defined (`packages/host-sdk/src/host/channels/session-self/index.ts:26`, `packages/host-sdk/src/host/channels/session-self/index.ts:29`); the binding is built with `makeIngressChannel` over `control.runs.rows()` (`packages/host-sdk/src/host/channels/session-self/index.ts:321`); `SessionSelfChannelsLive` provides the tag and also rebuilds `ChannelInventory` with the registration (`packages/host-sdk/src/host/channels/session-self/index.ts:356`, `packages/host-sdk/src/host/channels/session-self/index.ts:365`, `packages/host-sdk/src/host/channels/session-self/index.ts:367`). |
| `session.self.checkpoint` | ingress | Tag + Layer exists, but mirrored into `ChannelInventory` | Target + tag are defined (`packages/host-sdk/src/host/channels/session-self/index.ts:27`, `packages/host-sdk/src/host/channels/session-self/index.ts:33`); the binding is built with `makeIngressChannel` over `checkpointStream` (`packages/host-sdk/src/host/channels/session-self/index.ts:332`); the same `SessionSelfChannelsLive` provides the tag and inventory mirror (`packages/host-sdk/src/host/channels/session-self/index.ts:356`, `packages/host-sdk/src/host/channels/session-self/index.ts:366`, `packages/host-sdk/src/host/channels/session-self/index.ts:367`). |
| `session.log` | egress | Tag exists; no corresponding Live layer in the exported host surface | `SessionLogChannelTag` is defined (`packages/host-sdk/src/host/session-log-channel.ts:33`), and constructors return an egress channel with `append` (`packages/host-sdk/src/host/session-log-channel.ts:37`, `packages/host-sdk/src/host/session-log-channel.ts:45`); collection lowering uses `collection.insert` (`packages/host-sdk/src/host/session-log-channel.ts:58`). The host export surface exports the tag and constructors, but no `SessionLogChannelLive` (`packages/host-sdk/src/host/index.ts:128`). |
| `state.rows` / `stateChangesChannel*` | ingress | Tag exists; no corresponding Live layer in the exported host surface | `StateRowsChannel` is a `Context.Tag` (`packages/host-sdk/src/host/state-changes-channel.ts:18`); constructors build an ingress channel from `rows()` (`packages/host-sdk/src/host/state-changes-channel.ts:22`, `packages/host-sdk/src/host/state-changes-channel.ts:43`). The host export surface exports the tag and constructors, but no live layer (`packages/host-sdk/src/host/index.ts:111`). |
| `event.<name>` / `eventChannel*` | bidirectional | Inventory-style helper only in host-sdk export surface | The helper constructs a `BidirectionalChannel` with `rows()` and `append` (`packages/host-sdk/src/host/event-channel.ts:57`, `packages/host-sdk/src/host/event-channel.ts:60`, `packages/host-sdk/src/host/event-channel.ts:65`, `packages/host-sdk/src/host/event-channel.ts:68`), and collection lowering maps to `collection.rows` + `collection.insert` (`packages/host-sdk/src/host/event-channel.ts:78`, `packages/host-sdk/src/host/event-channel.ts:83`). The export surface lists only helpers/types, not a per-channel tag/live layer (`packages/host-sdk/src/host/index.ts:104`). |
| `dm.<handle>` / `notification.<handle>` | ingress + egress pair | Inventory-style helper only in host-sdk export surface | `humanChannelPair` constructs separate ingress and egress registrations (`packages/host-sdk/src/host/human-channel.ts:45`, `packages/host-sdk/src/host/human-channel.ts:62`, `packages/host-sdk/src/host/human-channel.ts:67`) and returns a `registrations` tuple (`packages/host-sdk/src/host/human-channel.ts:72`). `humanChannelRegistrations` flattens those registrations for inventory-style composition (`packages/host-sdk/src/host/human-channel.ts:112`), and the export surface lists helpers only (`packages/host-sdk/src/host/index.ts:117`). |
| Caller-supplied host channels | mixed | Inventory-style | `RuntimeHostTopologyOptions` accepts `channels?: ReadonlyArray<ChannelRegistration>` (`packages/host-sdk/src/host/types.ts:42`); `FiregridRuntimeHostLive` passes them into `SessionSelfChannelsLive` (`packages/host-sdk/src/host/layers.ts:297`, `packages/host-sdk/src/host/layers.ts:309`), where they are merged into `ChannelInventory` (`packages/host-sdk/src/host/channels/session-self/index.ts:367`). |
| Dark-factory sim channels (`factory.events`, `plan.ready`, `dm.operator`, `notification.operator`, `approval.operator`) | mixed | Tag + Layer exists in the sim, but still dual-wired through `ChannelInventory` | The sim defines per-channel `Context.Tag`s (`packages/tiny-firegrid/src/simulations/dark-factory/host.ts:90`, `packages/tiny-firegrid/src/simulations/dark-factory/host.ts:94`, `packages/tiny-firegrid/src/simulations/dark-factory/host.ts:98`, `packages/tiny-firegrid/src/simulations/dark-factory/host.ts:102`, `packages/tiny-firegrid/src/simulations/dark-factory/host.ts:106`), creates channel values (`packages/tiny-firegrid/src/simulations/dark-factory/host.ts:228`), builds a tag layer (`packages/tiny-firegrid/src/simulations/dark-factory/host.ts:307`), and also installs `ChannelInventoryLive` with the same registrations (`packages/tiny-firegrid/src/simulations/dark-factory/host.ts:326`, `packages/tiny-firegrid/src/simulations/dark-factory/host.ts:333`). |

## Static lowering check

The four `channel.ts` direction binding types map cleanly to the DurableTable primitive signatures as TypeScript shapes:

| Direction | Channel binding type | DurableTable primitive fit | Result |
| --- | --- | --- | --- |
| ingress | `TypedStreamBinding.stream: Stream.Stream<Row, unknown, never>` (`packages/host-sdk/src/host/channel.ts:40`) | `CollectionFacade.rows(): ProjectionStream<Row, DurableTableError>` (`packages/effect-durable-operators/src/DurableTable.ts:165`); `ProjectionStream` is a branded `Stream.Stream` (`packages/effect-durable-operators/src/DurableTable.ts:93`) | Clean static fit. Existing examples use `rows()` for session-self lifecycle (`packages/host-sdk/src/host/channels/session-self/index.ts:325`) and state rows (`packages/host-sdk/src/host/state-changes-channel.ts:53`). |
| egress | `AppendTargetBinding.append(payload): Effect<void, unknown, never>` (`packages/host-sdk/src/host/channel.ts:45`) | `insert(row): Effect<void, DurableTableError>` and `upsert(row): Effect<void, DurableTableError>` (`packages/effect-durable-operators/src/DurableTable.ts:166`, `packages/effect-durable-operators/src/DurableTable.ts:179`) | Clean static fit. Existing examples lower to `collection.insert` in `sessionLogChannelFromCollection` (`packages/host-sdk/src/host/session-log-channel.ts:58`) and event channels (`packages/host-sdk/src/host/event-channel.ts:84`). |
| callable | `CallTargetBinding.call(request): Effect<Response, unknown, never>` (`packages/host-sdk/src/host/channel.ts:52`) | Supports the SDD's durable RPC composition from `insertOrGet(row)` plus `rows()` (`packages/effect-durable-operators/src/DurableTable.ts:176`, `packages/effect-durable-operators/src/DurableTable.ts:165`), and direct query/read variants via `get` / `query` (`packages/effect-durable-operators/src/DurableTable.ts:181`, `packages/effect-durable-operators/src/DurableTable.ts:182`). | Clean static fit, but not enforced by the channel type. Example: dark-factory's `approval.operator` callable is currently an `Effect.succeed` stub rather than a DurableTable-backed binding (`packages/tiny-firegrid/src/simulations/dark-factory/host.ts:282`). |
| bidirectional | `BidirectionalChannel.binding` combines `stream` and `append` (`packages/host-sdk/src/host/channel.ts:81`) | Direct combination of `rows()` and `insert` / `upsert` (`packages/effect-durable-operators/src/DurableTable.ts:165`, `packages/effect-durable-operators/src/DurableTable.ts:166`, `packages/effect-durable-operators/src/DurableTable.ts:179`) | Clean static fit. Existing event-channel collection lowering wires `collection.rows` plus `collection.insert` (`packages/host-sdk/src/host/event-channel.ts:78`). |

The SDD's static model matches this: ingress lowers to `DurableTable.rows()` (`docs/sdds/SDD_FIREGRID_ONE_SUBSTRATE_PRIMITIVE.md:161`), egress to `insert` / `upsert` (`docs/sdds/SDD_FIREGRID_ONE_SUBSTRATE_PRIMITIVE.md:175`), callable to `insertOrGet` + `rows().filter().runHead` for durable RPC (`docs/sdds/SDD_FIREGRID_ONE_SUBSTRATE_PRIMITIVE.md:187`), and bidirectional to the ingress + egress combination (`docs/sdds/SDD_FIREGRID_ONE_SUBSTRATE_PRIMITIVE.md:212`).

## Implication for sim dispatch

Do not treat Cycle 0 as full-green outcome A. The correct gate result is B: dispatch only sims that can run against the existing per-channel tags/layers, and explicitly scope any sim that needs host-sdk exported channel tags to the currently available set (`session.self.lifecycle`, `session.self.checkpoint`, and dark-factory-local tags). The remaining host-sdk channel families are still constructor/registration/inventory shaped, and runtime agent-tool/MCP paths still resolve through `ChannelInventory`; any sim depending on per-channel Tag+Layer for `event.*`, human channels, `state.rows`, or `session.log` needs tf-kddg finish-line work or must carry that work in its slice before proving the projection claim.

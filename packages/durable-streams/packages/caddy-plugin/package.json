{
  "name": "@durable-streams-internal/caddy-plugin",
  "version": "0.1.0",
  "private": true,
  "description": "Caddy plugin for Durable Streams",
  "author": "Durable Stream contributors",
  "license": "Apache-2.0",
  "type": "module",
  "scripts": {
    "build": "go build -o caddy ./cmd/caddy",
    "test": "go test ./...",
    "test:run": "cd ../.. && vitest run --project caddy",
    "conformance": "pnpm build && cd ../.. && vitest run packages/caddy-plugin/test/conformance.test.ts"
  },
  "devDependencies": {
    "@durable-streams/server-conformance-tests": "workspace:*",
    "vitest": "^4.0.0"
  }
}

# @firegrid/protocol

Browser-safe Firegrid protocol schemas and state encoders shared by clients and runtime operators.

This package does not expose process execution, sandbox providers, or runtime-only services.

/**
 * Re-export of the unified table from `@firegrid/runtime/unified`.
 * See `signal.ts` in this folder for the Phase 2 migration context.
 */

export {
  peerEventKey,
  PeerEventRowSchema,
  permissionKey,
  PermissionRequestRowSchema,
  scheduleKey,
  ScheduledRowSchema,
  UnifiedTable,
  type UnifiedTableService,
  webhookFactKey,
  WebhookFactRowSchema,
} from "@firegrid/runtime/unified"

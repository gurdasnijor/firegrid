# Firegrid

Canonical design source:

- [Firegrid Architecture and Operation Messaging Boundary](docs/SDD_FIREGRID_ARCHITECTURE_AND_INVOCATION_BOUNDARY.md)
- [Durable Agent Substrate SDD](docs/SDD_DURABLE_AGENT_SUBSTRATE.md)
- [Docs map](docs/README.md)

Other canonical SDDs:

- [Client Event Planes And State Producers](docs/SDD_CLIENT_EVENT_PLANES_AND_STATE_PRODUCERS.md)
- [Choreography Facade](docs/SDD_CHOREOGRAPHY_FACADE.md)
- [Launchable Substrate Host And Lab](docs/SDD_LAUNCHABLE_SUBSTRATE_HOST_AND_LAB.md)

Acai specs live under `features/firegrid/` for Firegrid protocol, client,
runtime, app, and remediation requirements.

## Current State

The repo uses Firegrid product and package vocabulary:
`@firegrid/protocol` (browser-safe shared schemas), `@firegrid/client`
(browser/app-facing launch client), `@firegrid/runtime` (server-side durable
workflow and launch runtime), and `apps/flamecast` (clean-room tracer app).

Run the canonical ready-for-review gate when preparing a PR:

```sh
pnpm verify
```

Implementation work starts from the Acai specs and includes full
Acai ACID references in test names or nearby comments.

## Architecture Evidence

Dependency graph evidence is generated by dependency-cruiser and committed as
Mermaid files under `docs/`. Refresh it with:

```sh
pnpm run arch:deps
```

The strict CI boundary gate is:

```sh
pnpm run lint:deps
```

Generated artifacts:

- [Workspace dependency graph](docs/dependency-graph.mmd)
- [Client package dependency graph](docs/dependency-graph-client.mmd)
- [Protocol package dependency graph](docs/dependency-graph-protocol.mmd)
- [Runtime package dependency graph](docs/dependency-graph-runtime.mmd)
- [Flamecast tracer app dependency graph](docs/dependency-graph-flamecast.mmd)

## Run The Runtime

Run the Firegrid runtime against an existing Durable Streams endpoint by setting
`DURABLE_STREAMS_URL`:

```sh
DURABLE_STREAMS_URL=https://... pnpm --filter @firegrid/runtime run firegrid
```

feature:
  name: launchable-substrate-host
  product: firegrid
  description: Define the launchable client, host process, and lab surfaces needed to run prototype durable agent runtimes without adding a host mutation control plane.

components:
  CLIENT_SURFACE:
    requirements:
      1: The substrate client is the normal application-facing writer surface for durable intents.
      2: The first substrate client surface is Effect-native.
      3: The client can declare durable work through existing substrate producer semantics.
      4: The client can create choreography intents through the choreography facade.
      5: The client can emit rows to registered event planes.
      6: The client exposes curated read handles for substrate and event-plane projections.
      7: The client root does not expose raw stream append, raw StreamDB collections, raw durable row builders, or raw Durable Streams State envelopes.
      8: Operator, testing, and diagnostic escape hatches live under explicit subpaths rather than the production root.
      9: Client read handles expose explicit snapshot, stream, and until operations so one-shot reads are distinct from live subscriptions.
      10: Client snapshot operations read the current no-gap materialized view once and do not mutate durable state.
      11: Client work declaration accepts idempotency metadata and caller input while keeping idempotency metadata out of the durable.run row value.
      12: Caller input for client-declared work is represented as substrate-generic durable.run data rather than a Fireline, Firepixel, ACP, MCP, session, prompt, provider, sandbox, or transport row family.
      13: The v1 client choreography surface exposes scheduleAt only; sleep, waitFor, and awaitAwakeable remain server-side runtime primitives requiring CurrentWorkContext.
      14: Client event-plane APIs take an EventPlane definition supplied at the call site and do not maintain a global plane registry.
      15: Event-plane read handles use the plane projection service for snapshot, stream, and until operations.

  CLIENT_COMPATIBILITY:
    requirements:
      1: Non-Effect wrappers are deferred until a real external client call site requires them.
      2: Future non-Effect wrappers must be thin wrappers over the Effect-native client and must not fork durable semantics.
      3: Future non-Effect wrappers must not expose runtime suspension semantics until a real call site requires that presentation.
      4: The client service exposed inside host development helpers is the same capability as the standalone Effect-native client.

  HOST_PROCESS:
    requirements:
      1: The substrate host is a launchable process that starts or connects to a Durable Streams endpoint.
      2: The host runs configured substrate subscribers and operator programs from durable streams/state.
      3: The host can run timer and scheduled-work subscribers.
      3-note: The first launchable subscriber slice covers timer and scheduled-work subscribers as scoped, subscription-driven host programs enabled by the Host Program Graph. Single-shot scan functions remain catch-up/terminalization building blocks; the host runner should wake from durable stream/state changes or due-time deadlines rather than naive fixed-interval polling.
      4: The host can run projection-match subscriber programs supplied by the Host Program Graph.
      5: The host can run claim-before-side-effect operator programs supplied by the Host Program Graph.
      6:
        requirement: The first host lifecycle status surface is in-process and read-only; network diagnostics are deferred.
        deprecated: true
      6-note: Host diagnostics and local lifecycle status are deferred beyond the core launchable lab slice.
      7: The host shuts down through Effect scopes and process-signal handling.
      8:
        requirement: The first host package owns embedded Durable Streams dev-server startup rather than requiring a separate CLI or dev package.
        deprecated: true
      8-note: Firegrid no longer owns Durable Streams dev-server lifecycle; host/runtime code attaches to an externally managed stream URL.

  HOST_CONFIGURATION:
    requirements:
      1:
        requirement: The host supports an embedded-dev boot plan that starts a local Durable Streams server.
        deprecated: true
      2: The host supports an attached boot plan that joins an existing Durable Streams endpoint.
      3: Host boot plans can be built from explicit options.
      4: Host boot plans can be decoded from Effect Config.
      5:
        requirement: Missing stream URL selects embedded-dev mode rather than requiring a remote service in local development.
        deprecated: true
      5-note: Missing stream URL is now a configuration error; local development must start Durable Streams outside Firegrid.
      6: Configured stream URL selects attached mode and does not start or own the remote Durable Streams process.
      7: Process identity is generated by default; explicit process ids are advanced or test configuration.
      8:
        requirement: Authorization and token config materialize stream request headers without exposing secret values through durable rows.
        deprecated: true
      8-note: Auth/header transport is deferred beyond the core launchable lab slice.
      9:
        requirement: Host diagnostics never return secret values or authorization headers.
        deprecated: true
      9-note: Host diagnostics and auth secret handling are deferred beyond the core launchable lab slice.
      10:
        requirement: When both authorization and bearer token config are present, authorization config wins.
        deprecated: true
      10-note: Auth/header transport is deferred beyond the core launchable lab slice.
      11:
        requirement: Bare token config materializes as an Authorization Bearer header.
        deprecated: true
      11-note: Auth/header transport is deferred beyond the core launchable lab slice.

  RUNTIME_COMPOSITION:
    requirements:
      1: Agent-runtime layers provide a Host Program Graph to the substrate host rather than adding host mutation endpoints.
      2: The Host Program Graph composes event-plane layers, projection-match subscriber program layers, operator program layers, and adapter service layers.
      3: Host Program Graph definitions are named ordinary Effect Layer compositions supplied in process.
      4: Serialized config may select known local Host Program Graph definitions but does not create a mutable global runtime registry.
      5: Development helpers can compose client and host layers in one Effect program without making that the production process boundary.
      5-note: Such test/dev helpers must remain test-scoped and must not become a public runtime API. The original `withHost`-style public helper has been removed (`firegrid-runtime-process.RUNTIME_PACKAGE.4`).
      6:
        requirement: withHost-style development helpers may provide the substrate client to the program they run.
        deprecated: true
      6-note: Public `withHost` is removed. Test helpers may locally compose client and host layers but must not be exported from `@firegrid/runtime` as an app-client provider (`firegrid-runtime-process.RUNTIME_PACKAGE.4`).
      7:
        requirement: withHost-style development helpers do not create a distinct writer API from the standalone substrate client.
        deprecated: true
      7-note: Subsumed by the removal of public `withHost` (`firegrid-runtime-process.RUNTIME_PACKAGE.4`).
      8: Development Host Program Graph discovery uses explicit caller-supplied local maps rather than dynamic module import.
      9: Host Program Graph definitions are not fetched from durable state.

  SERVER_RUNTIME_API:
    requirements:
      1: Server-side durable behavior is authored with substrate runtime primitives such as Choreography, Projection, Work, EventPlane, subscribers, and operator helpers.
      2: Server-side runtime primitives remain in the substrate foundation and Host Program Graph layers rather than in the standalone client root.
      3: The launchable host runs server-side runtime primitives supplied by the Host Program Graph; it does not require applications to call host mutation endpoints.

  LAB_INSPECTOR:
    requirements:
      1: The lab is an example or development application built on the same substrate client used by other consuming runtimes.
      2: The lab supports replay plus live-follow views for stream-shaped data.
      3: The lab can use Durable Streams registry-style discovery for stream lists.
      4: The lab renders structured JSON stream records and projected state in substrate-specific panels.
      5: The lab starts scenarios by calling the substrate client, not by calling host mutation endpoints.
      6: The lab does not receive privileged write authority beyond the substrate client.
      7: Raw stream views in the lab are diagnostic panels and are visually or structurally separated from scenario controls.
      8: The first lab is a small Vite React application rather than a terminal-only inspector.
      9: The first lab uses React with vanilla CSS or CSS modules and does not introduce a UI framework dependency.

  SCENARIOS:
    requirements:
      1: The first example program proves sleep through client intent, host timer subscriber, ready work derivation, operator completion, and lab observation.
      2: A scheduled-work example program proves scheduleAt creates future work intent without blocking the caller.
      3: A fake permission or ACP-shaped example program proves a caller-owned event plane can drive waitFor through projection-match completion resolution without making ACP substrate-native.
      4: A tool-call-shaped example program proves event-plane observation plus claim-before-side-effect operator execution without making tool calls substrate-native.
      5: Example programs are ordinary Effect programs runnable from both command line and lab UI while using the same substrate client APIs.
      6: First-slice example program entries live under apps/lab and are not exported from the production client root.
      7: The example command shape is `pnpm --filter lab scenario <name>`.

  PACKAGING:
    requirements:
      1: The first launchable design introduces package boundaries for packages/client and packages/host plus the standalone app boundary for apps/lab.
      2: The substrate package remains the foundation library and does not become the launchable process.
      3: The client package is the normal application dependency.
      4: The host package owns launchable process wiring.
      5: The lab app owns development inspection and scenario workbench UI.
      6:
        requirement: The host package exposes thin constructors for attached, attachedFromConfig, embeddedDev, bootPlanFromConfig, and withHost-style composition.
        deprecated: true
      6-note: withHost-style composition belongs in packages/host and remains Effect-native; it is not a separate CLI package and not an HTTP control plane.
      7: The client package exposes an Effect-native root API.
      8: The host package exposes an Effect-native launch and composition API.
      9: A separate CLI or dev package is deferred until host process-launch concerns outgrow packages/host.

  HOST_DIAGNOSTICS:
    requirements:
      1:
        requirement: The minimum in-process diagnostics include health, version, process id, boot mode, stream identity, active Host Program Graph names, subscriber program liveness, operator program liveness, uptime, process metrics, and non-secret error summaries.
        deprecated: true
      1-note: Host diagnostics are deferred beyond the core launchable lab slice.
      2:
        requirement: Host diagnostics do not start scenarios or mutate durable streams/state.
        deprecated: true
      2-note: Host diagnostics are deferred beyond the core launchable lab slice.
      3:
        requirement: The first host diagnostics surface is in-process rather than HTTP.
        deprecated: true
      3-note: Host diagnostics are deferred beyond the core launchable lab slice.
      4:
        requirement: The host does not allocate a default diagnostics port while network diagnostics are deferred.
        deprecated: true
      4-note: Host diagnostics are deferred beyond the core launchable lab slice.
      5: Future network diagnostic routes, if introduced, must be read-only and must not mutate durable streams/state.

constraints:
  NO_CONTROL_PLANE:
    requirements:
      1: The host does not expose application mutation endpoints such as start-work, resolve-permission, or run-scenario.
      2: Application and lab writes go through the substrate client durable intent surface.
      3: Read-only host diagnostics do not mutate durable streams/state.
      4: The lab does not depend on hidden host writer APIs for scenario execution.
      5: The lab is not a privileged substrate layer or host backend.

  AUTHORITY_BOUNDARY:
    requirements:
      1: Durable Streams and Durable State remain the source of truth.
      2: The host does not become a second authority for runs, completions, claims, or event-plane rows.
      3: Event-plane projections do not replace substrate claim authority or completion authority.
      4: The launchable layer does not introduce new substrate-native row families for Fireline, Firepixel, ACP, MCP, sessions, prompts, providers, sandboxes, or process transports.

  SCHEMA_OWNERSHIP:
    requirements:
      1: The package that owns a durable row family owns the Effect Schema and TypeScript type source of truth for that row family.
      2: Client and host code reuse substrate-owned row schemas, types, state helpers, and transition builders rather than redefining durable.run, durable.completion, durable.claim.attempt, or durable.trace shapes.
      3: Caller-owned event-plane schemas live with the runtime or application that owns the event plane and are supplied through EventPlane definitions.
      4: Host-local configuration, diagnostics, and lifecycle types remain host-owned and are not durable substrate row families.
      5: The launchable layer does not introduce a shared types package for substrate-owned or caller-owned row schemas.

  PRIOR_ART:
    requirements:
      1: Durable Streams test UI is prior art for registry discovery, live follow, catchup, JSON rendering, and stream inspection.
      2: Durable Streams test UI is not prior art for substrate command endpoints.
      3: Fireline client is prior art for a narrow app-facing client with curated reads and explicit operator/testing subpaths.
      4: Restate is prior art for separating a server-side runtime SDK, a standalone client SDK, and a launchable runtime or server.
      5: Restate is not prior art for making the substrate host a central mutable ingress API.
      6: Flink is prior art for treating the executable runtime shape as a program graph of sources, operators, and sinks run by a launchable host, without making that graph a host mutation API.

{
  "name": "@firegrid/scenarios",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "direct-state-clock-receiver": "tsx src/cli.ts direct-state-clock-receiver",
    "direct-state-clock-receiver:self-test": "tsx src/cli.ts direct-state-clock-receiver --self-test",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@durable-streams/client": "^0.2.3",
    "@durable-streams/server": "^0.3.1",
    "@durable-streams/state": "^0.2.5",
    "@firegrid/runtime": "workspace:*",
    "effect": "^3.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.20.4",
    "vitest": "^3.2.0"
  }
}
